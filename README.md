# WebBasicDesigner

A browser-based Visual Basic-style application designer built with JavaScript.

WebBasicDesigner allows users to visually build fake desktop-style applications directly in the browser by dragging, placing, and configuring UI elements such as buttons, labels, and textboxes.
